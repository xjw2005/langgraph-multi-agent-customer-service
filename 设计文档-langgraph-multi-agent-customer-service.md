# 设计文档：langgraph-multi-agent-customer-service  
**高端母婴市场 · 多 Agent 智能客服系统**

| 文档信息 |  |
|----------|--|
| 版本 | v1.0 |
| 状态 | 草案（可随实现迭代） |
| 技术主线 | LangGraph · 多 Agent · RAG · 记忆与检查点 |

---

## 1. 背景与目标

### 1.1 业务背景

整体生育率承压的背景下，**高收入家庭**仍是高端母婴品类的核心客群。该群体对服务体验、信息可信度与决策效率要求更高，愿意为**品质、溯源、专业背书**付费，而非单纯追求「性价比」叙事。

### 1.2 产品定位

构建面向高端母婴场景的 **AI 客服与导购助手**，在对话中体现：

- **高端沟通范式**：弱化「便宜/划算」，强调品质、品控、正品保障、国际供应链与专业建议；语气稳重、可信赖。
- **极端可靠性**：严控幻觉；产品介绍、成分说明、售后政策等与知识库/官方口径一致，避免品牌与合规风险。
- **强 RAG + 精准推荐**：基于检索快速对齐参数、适用场景与用户反馈摘要，缩短高净值用户的决策路径，倾向「一步到位」的推荐与解释。

### 1.3 设计目标（可验收）

| 维度 | 目标 |
|------|------|
| 准确性 | 事实类回答可追溯到知识片段；禁止编造 SKU、价格、政策条文 |
| 体验 | 多轮自然、有「私人顾问」感；尊重用户偏好与禁忌 |
| 可运维 | 流程图式编排，子能力可独立替换与 A/B |
| 韧性 | 支持会话级状态持久化与故障恢复（Checkpoint） |

### 1.4 范围与非目标

**范围内**：售前咨询、选品建议、成分/适用场景问答、活动话术辅助、会话记忆与偏好沉淀、与现有订单/会员系统对接（接口层预留）。

**范围外（首期可不做）**：支付闭环、医疗诊断级建议、自动退换货执行（仅政策解读与引导）。

---

## 2. 总体架构

### 2.1 逻辑分层

```
┌─────────────────────────────────────────────────────────┐
│  接入层：Web / App / 企业微信等 · 鉴权 · 限流 · 审计      │
├─────────────────────────────────────────────────────────┤
│  Orchestration：LangGraph 主图 · 路由 · 子图调用 · 汇总   │
├──────────────┬──────────────┬──────────────┬──────────────┤
│ 选品子 Agent │ 专业问答子    │ 推销子 Agent │ 工具/RAG     │
│              │ Agent        │              │ 记忆读写      │
├──────────────┴──────────────┴──────────────┴──────────────┤
│  数据层：向量库 · 结构化商品/政策库 · 会话与画像存储       │
└─────────────────────────────────────────────────────────┘
```

### 2.2 多 Agent 职责

| 角色 | 职责 | 输入侧重 | 输出侧重 |
|------|------|----------|----------|
| **总 Agent（Master）** | 意图理解、任务拆解、调度子 Agent、冲突消解、**最终答复合成** | 用户当前 utterance + 状态 + 记忆摘要 | 面向用户的一条连贯回复（可含结构化推荐卡片占位） |
| **选品子 Agent** | 在约束条件下筛选 SKU、对比维度、适用人群/场景 | 需求、预算区间、月龄、过敏史等 | 候选列表 + 每条理由（引用检索证据） |
| **专业问答子 Agent** | 成分、用法、规格、认证、与竞品差异等客观问题 | 问题焦点 + RAG 上下文 | 短答/长答 + 引用来源标识 |
| **推销子 Agent** | 在合规前提下生成高转化话术（活动、套装、会员权益） | 当前候选品 + 活动规则片段 | 话术片段（由总 Agent 决定是否采用及顺序） |

**协作原则**：「**对的问交给对的人**」—— 总 Agent 只做轻量推理与编排，不重写专业事实；事实以 RAG 与结构化库为准；推销内容不得覆盖或篡改事实子 Agent 的结论。

### 2.3 LangGraph 主图（概念）

- **节点**：`classify_intent` → `route` → `{selection | qa | promo | hybrid}` → `aggregate` → `respond`；可选 `human_review`（高风险类目）。
- **边**：由路由函数根据意图、置信度、是否缺 mandatory slot（如过敏信息）决定。
- **子图**：每个子 Agent 可独立为 `CompiledSubgraph`，便于单测与版本化。

### 2.4 分布式多 Agent（可选演进）

首期可在**单进程多子图**完成；当 QPS 或模型路由复杂时：

- 子 Agent 以 **异步任务队列 / 独立微服务** 暴露，Master 通过 RPC 调用并聚合；LangGraph 侧以 `async` 节点包装。
- 需统一 **trace_id**、**会话 id**、**截止时间** 与 **部分结果缓存**，避免重复检索与重复计费。

---

## 3. 状态模型与 Checkpoint

### 3.1 图状态（建议字段）

与实现语言无关，核心字段示例：

- `messages`：对话消息列表（多模态可扩展）。
- `intent`、`slots`：意图与槽位（月龄、品类、价格带、禁忌等）。
- `rag_contexts`：本轮检索到的文档块 ID 与摘要指针。
- `sub_results`：各子 Agent 结构化输出（JSON schema 约束）。
- `final_answer_draft` / `policy_flags`：合规标记、需人工复核原因。
- `user_profile_ref`：画像与长期记忆的外部存储键。

### 3.2 短期记忆 vs 长期记忆

| 类型 | 内容 | 载体 | 生命周期 |
|------|------|------|----------|
| 短期 | 当前会话多轮上下文、未完成槽位、本轮检索缓存 | 图状态 + Checkpoint | 会话级 |
| 长期 | 偏好品类、常用品牌、孩子月龄更新、过敏史、沟通风格 | 用户画像库 / KV + 可选向量摘要 | 用户级，可 TTL 与纠错 |

**体验目标**：长期记忆用于开场与推荐默认策略；**回答事实仍以 RAG 为准**，记忆不直接作为事实源。

### 3.3 Checkpoint 策略

- **持久化后端**：开发可用内存/SQLite；生产建议 Postgres + LangGraph 官方/checkpoint 适配实现。
- **恢复场景**：网关超时重试、用户刷新页面、子 Agent 调用失败重放。
- **隐私**：Checkpoint 中避免明文身份证等；敏感字段引用外部安全存储。

---

## 4. 数据与 RAG 设计

### 4.1 知识来源

- **结构化**：SKU、规格、价格带、库存状态、活动规则（版本号+生效时间）。
- **半结构化**：成分表、使用说明、FAQ、售后政策条文。
- **非结构化**：评测摘要、用户反馈聚合（需脱敏与统计口径说明）。

### 4.2 检索流程

1. **查询改写**：结合意图与槽位生成检索 query（可小模型 + 规则）。
2. **混合检索**：关键词（BM25）+ 向量；商品 ID 过滤优先。
3. **重排**：交叉编码器或轻量重排模型，控制上下文窗口。
4. **引用约束**：生成阶段强制「仅使用所给 context」；context 不足时 **明确拒绝或转人工**，而非编造。

### 4.3 与「反幻觉」配套的工程手段

- 答案侧 **引用块 ID** 或「来源：商品页/政策 v3.2」。
- **数值类**（价格、保质期）优先走结构化 API，不走纯生成。
- 定期 **离线评测**：黄金问题集 + 事实一致性自动检查。

---

## 5. Prompt 与话术策略

### 5.1 分层 Prompt

- **系统层**：品牌调性、禁止项（医疗承诺、贬低竞品、虚假折扣）。
- **角色层**：总控 / 选品 / 问答 / 推销各自独立 system prompt。
- **动态层**：注入用户画像摘要、当前活动规则片段、RAG 块。

### 5.2 高端调性要点（摘要）

- 用语：专业、克制、可验证；多用「适合」「通常建议」「以说明书/官方说明为准」。
- 避免：过度口语化套路、强压单、与检索矛盾的超绝对化表述。

### 5.3 合成策略（总 Agent）

- 先合并 **事实与选品结论**，再决定是否叠加 **推销话术**；二者冲突时以事实与子 Agent 为准。

---

## 6. 接口与集成

### 6.1 对外 API（示例）

- `POST /v1/chat`：流式 SSE 返回 token + 可选结构化 `citations`、`product_cards`。
- `GET /v1/session/{id}`：恢复会话（依赖 Checkpoint）。
- `POST /v1/feedback`：点赞/点踩/纠错，用于 RAG 与 prompt 迭代。

### 6.2 内部依赖

- 商品/库存/活动：**内部 REST/gRPC**（带缓存与熔断）。
- 会员画像：只读 + 最小必要字段。

---

## 7. 非功能需求

| 类别 | 要求 |
|------|------|
| 延迟 | P95 首 token < 约定值（依模型与区域调整）；长检索可流式「正在为您查询」 |
| 可用性 | 子 Agent 失败降级：问答 → 缩小 RAG；选品 → 返回保守推荐 + 人工入口 |
| 安全 | 鉴权、租户隔离、日志脱敏、提示注入防护（系统指令与工具边界） |
| 合规 | 广告法、婴幼儿相关宣传禁区、个人信息保护法 |

---

## 8. 可观测性与运维

- **Tracing**：OpenTelemetry，串联 Master 与各子调用与检索。
- **指标**：检索命中率、空检索率、人工转接率、幻觉举报率。
- **版本**：Prompt 版本、知识库版本、子图版本与灰度发布。

---

## 9. 风险与对策

| 风险 | 对策 |
|------|------|
| RAG 过时 | 版本号、定时同步、答案中展示知识切片时间 |
| 多 Agent 结论冲突 | Master 显式优先级规则 + 记录冲突日志 |
| 推销过度 | 独立推销子 Agent + 合规后处理 + 用户负反馈回路 |

---

## 10. 实施里程碑（建议）

1. **M1**：Master 单图 + 专业问答子 Agent + RAG + Checkpoint 会话恢复。  
2. **M2**：选品子 Agent + 结构化商品 API + 引用展示。  
3. **M3**：推销子 Agent + 活动规则 RAG + 画像长期记忆。  
4. **M4**：评测体系、灰度、可选分布式子 Agent。

---

## 11. 技术栈（与图片一致并细化）

- **编排与状态**：LangGraph（图编排、子图、Checkpoint）。
- **模型**：主对话模型 +（可选）小模型做分类/改写/重排。
- **检索**：向量数据库 + 关键词检索；重排层。
- **存储**：会话 Checkpoint、画像库、审计日志。
- **部署**：容器化；生产建议与网关、向量库、业务 API 分域部署。

---

## 12. 附录：子 Agent 输出 JSON Schema（示例草案）

**选品子 Agent**（片段）：

```json
{
  "candidates": [
    {
      "sku_id": "string",
      "title": "string",
      "rationale": "string",
      "evidence_ids": ["string"]
    }
  ],
  "needs_user_clarification": ["string"]
}
```

**专业问答子 Agent**：

```json
{
  "answer": "string",
  "citations": [{ "doc_id": "string", "excerpt": "string" }],
  "confidence": "high|medium|low",
  "escalate_to_human": false
}
```

**推销子 Agent**：

```json
{
  "promo_lines": ["string"],
  "constraints_ok": true,
  "notes": "string"
}
```

---

*本文档由需求说明整理而成，实现阶段可与代码仓库中的模块命名、状态定义对齐后做增量修订。*
